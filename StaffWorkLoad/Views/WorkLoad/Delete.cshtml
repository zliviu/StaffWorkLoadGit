@model StaffWorkLoad.Models.WorkLoad

@{
    ViewBag.Title = "Delete Workload";
}

@*<h2>Delete</h2>*@

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Workload</legend>

    <div class="display-label">Staff Name</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.StaffName.FullName)
    </div>

    <div class="display-label">Workload Start Date</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.StartDate)
    </div>

    <div class="display-label">Workload End Date</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.EndDate)
    </div>

    <div class="display-label">Workload Type</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.WorkType)
    </div>

    <div class="display-label">Workload Notes</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Notes)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
