@model StaffWorkLoad.Models.Person

@{
	ViewBag.Title = "Staff Member Details";
}

<h2>Details</h2>

<fieldset>
    <legend>Staff Member</legend>

    <div class="display-label">First Name</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.FirstName)
    </div>

    <div class="display-label">Last Name</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.LastName)
    </div>

    <div class="display-label">Legend Colour</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.LineColour)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.PersonID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
